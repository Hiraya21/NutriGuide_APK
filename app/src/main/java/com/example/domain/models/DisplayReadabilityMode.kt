package com.example.domain.models

enum class DisplayReadabilityMode(val displayName: String, val shortName: String) {
    STANDARD("Standard", "Standard"),
    HIGH_CONTRAST_XL("High Contrast / Extra Large", "Sunlight ☀️ / XL")
}
